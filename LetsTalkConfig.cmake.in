@PACKAGE_INIT@

message("Adding ${CMAKE_CURRENT_LIST_DIR} to prefix path")
include(${CMAKE_CURRENT_LIST_DIR}/IdlTarget.cmake)

find_package(fastdds)
find_package(foonathan_memory)

include ( "${CMAKE_CURRENT_LIST_DIR}/LetsTalkTargets.cmake" )
